<UserControl
	x:Name="RootLayout"
	x:Class="Wautilus.ArticleControls.ArticlesWrap"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:Wautilus.ArticleControls"
	mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="300"
	>
	
	<UserControl.Resources>

        <DataTemplate x:Key="ArticleTemplate">
            <Label Content="{Binding Path=Label}" />
        </DataTemplate>
		
        <ItemsPanelTemplate x:Key="PanelTemplate">
			<WrapPanel />
        </ItemsPanelTemplate>
		
	</UserControl.Resources>
	
    <ListBox
		x:Name="MainBox" SelectionMode="Extended"
		ItemsSource="{Binding Path=Articles}"
		ItemTemplate="{StaticResource ResourceKey=ArticleTemplate}"
		ItemsPanel="{StaticResource ResourceKey=PanelTemplate}"
		ScrollViewer.HorizontalScrollBarVisibility="Disabled"
		ScrollViewer.VerticalScrollBarVisibility="Auto"
	/>
	
</UserControl>